<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.garehn.lyceesgt"
    android:versionCode="1"
    android:versionName="alpha 1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="CHOISIR SON LYCÉE"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PriorityActivity"></activity>
        <activity android:name=".RankActivity" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".SizeActivity" />
        <activity android:name=".SpecialityActivity" />
        <activity android:name=".DistanceActivity" />
        <activity android:name=".LangueActivity" />
        <activity android:name=".TechnoActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>